import React from "react";
import { View } from 'react-native';

const TabFiveScreen = () => {
    return (
        <View style={{ backgroundColor: '#00f', height: 100, width: 100, borderRadius: 15 }} />
    );
};

export default TabFiveScreen;